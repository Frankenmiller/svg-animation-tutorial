const logo = document.querySelectorAll('#ETH-logo path');
const ethereum = document.querySelectorAll('#ethereum path');
const designer = document.querySelectorAll('#logo path');

console.log(designer);

// for (let i=1; i<designer.length; i++) {
//     console.log(`Path ${i} is ${designer[i].getTotalLength()}`);
// }